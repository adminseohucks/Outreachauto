# ===========================================================================
# LinkedPilot v2 - VPS Environment Variables
# ===========================================================================
# Copy this file to .env and fill in the values:
#   cp .env.example .env
#   nano .env
# ===========================================================================

# API key shared between the Chrome extension and this server.
# Generate a strong random key:  python3 -c "import secrets; print(secrets.token_hex(32))"
VPS_API_KEY=changeme_to_a_random_64char_secret
