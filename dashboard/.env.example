# LinkedPilot v2 Configuration

# Dashboard
DASHBOARD_HOST=0.0.0.0
DASHBOARD_PORT=8080
SECRET_KEY=change-me-to-a-random-64-char-string

# OpenOutreach CRM Database path (READ ONLY)
CRM_DB_PATH=../openoutreach/assets/data/crm.db

# LinkedPilot Database path
LINKEDPILOT_DB_PATH=./data/linkedpilot.db

# VPS AI Server (Ollama on 50.6.202.231)
# Generate key: python3.11 -c "import secrets; print(secrets.token_hex(32))"
# Same key must be set in /var/www/linkedpilot-ai/.env on VPS
VPS_AI_URL=https://50.6.202.231:8443/api/suggest-comment
VPS_API_KEY=change-me-to-a-64-char-api-key
VPS_HEALTH_URL=https://50.6.202.231:8443/health
VPS_SSL_VERIFY=false

# Work Hours (IST)
WORK_HOUR_START=9
WORK_HOUR_END=18
WORK_DAYS=0,1,2,3,4

# Like Engine Limits
LIKE_DAILY_LIMIT=100
LIKE_WEEKLY_LIMIT=300
LIKE_MIN_DELAY=240
LIKE_MAX_DELAY=840

# Comment Engine Limits
COMMENT_DAILY_LIMIT=50
COMMENT_WEEKLY_LIMIT=200
COMMENT_MIN_DELAY=480
COMMENT_MAX_DELAY=1320

# Ramp-up (first 2 weeks: 30% of limits)
RAMP_UP_WEEKS=2
RAMP_UP_PERCENTAGE=30

# Senders (up to 4)
MAX_SENDERS=4
